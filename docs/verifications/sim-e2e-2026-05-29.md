# sim 端到端真实跑通验证(2026-05-29)

scripts/sim 多租户业务模拟器(ta/tb/tc)+ 4 类 worker + trigger + orchestrator 端到端真实跑通验证报告。包含正常路径、异常路径、修复的 BE bug、open backlog。

## 1. 验证范围

### 1.1 6 BE 模块全覆盖

| 模块 | pipeline 形态 | 验证 |
|---|---|---|
| `batch-trigger` | API trigger → trigger_outbox → Kafka | ✓ 100+ 触发,Idempotency-Key / X-Internal-Secret / dryRun / 6 种 triggerType |
| `batch-orchestrator` | trigger_outbox 消费 → job_instance → job_task → outbox_event → Kafka | ✓ + DAG advance + dead letter |
| `batch-worker-import` | RECEIVE → PREPROCESS → PARSE → VALIDATE → LOAD | ✓ ta/tb/tc 全 SUCCESS,biz 表 3×3 行落地 |
| `batch-worker-export` | PREPARE → GENERATE → STORE → REGISTER → COMPLETE | ✓ ta/tb/tc 全 SUCCESS,MinIO outbound 3 csv |
| `batch-worker-dispatch` | PREPARE → DISPATCH → ACK → RETRY → COMPENSATE → COMPLETE | ✓ tc 全通(MockServer 收 POST),tb 实际投递(状态机告警),ta SFTP reach |
| **`batch-worker-process`** | PREPARE → COMPUTE → VALIDATE → COMMIT → FEEDBACK(WAP) | ✓ TA_PROCESS_CUSTOMER_SUMMARY,biz.customer_processed 3 行 |

### 1.2 异常路径覆盖

| # | 场景 | 关键 | 结果 |
|---|---|---|---|
| L1 | dry-run | ADR-026 | ✓ dry_run=t,biz delta=0,instance SUCCESS |
| L2 | 幂等冲突 | 同 idempotencyKey 二次触发 | ✓ 返回同 instanceNo |
| L3 | bad CSV | short row → LOAD NULL violation | ✓ IMPORT_LOAD_FAILED |
| L4 | DISPATCH 失败 | channel_config.sftp_host missing | ✓ DISPATCH_SEND_FAILED → channel circuit OPEN → retry tick |
| L5 | mid-task lease 复活 | kill -9 worker 中途 | ✓ instance 卡 RUNNING → 60s lease 失效 → 重启 worker → task 被 reclaim → 新 worker SUCCESS |
| L6 | 超大 payload | 3MB IMPORT base64 | ✓ Kafka `RecordTooLargeException`(trigger→Kafka 层拦截,早于 worker IMPORT_RECEIVE 检查),trigger_outbox 卡 PUBLISHING |
| L7 | CATCH_UP + RERUN | 历史 bizDate + 重跑 | ✓ 两类型 SUCCESS,trigger_type 字段持久化 |
| L8 | dead letter | 异常自然累积 | ✓ `batch.dead_letter_task` 1681 条记录,replay_status/replay_count/next_replay_at 完整 |
| L9 | workflow FAILED branch | child instance fail | ✓ child PROCESS FAILED → workflow_node_run FAILED with error_code → workflow_run FAILED → 父 instance FAILED(状态机三层 propagate) |

### 1.3 Workflow DAG(M)

`TA_WF_E2E`: START → NODE_PROCESS(TA_PROCESS_CUSTOMER_SUMMARY) → END

- workflow_run 状态机:CREATED → RUNNING → SUCCESS / FAILED
- workflow_node_run:START / JOB / END 三 node_run 全持久化
- 子 instance 自动 spawn(parent_instance_id 关联)
- DAG advance 由 trigger 链路 + DefaultPartitionDispatchService.dispatchInitialDagNodes 驱动

## 2. 修复的 BE bug(已写代码)

### 2.1 `ImportIngressScanner` 单租户硬绑(M+)

**问题**:Scanner 启动时读 `workerConfiguration.tenantId()` 单一 tenant,所有 MinIO 对象登记的 file_record 都打这一个 tenantId。多租户场景无法用一个 worker-import 同时服务 ta/tb/tc。

**修复**:`batch-worker-import/.../runtime/ImportIngressScanner.java` 加 `resolveTenantFromObjectName(objectName)` — 按 MinIO prefix 解析 tenantId(`ingress/ta/foo.csv` → `ta`),解析不到时 fallback worker 自身 tenantId(保持旧布局兼容)。`tryRegister` 内 5 处 `workerConfiguration.tenantId()` 全替换为 `resolvedTenant`。

**验证**:投放 `ingress/{ta,tb,tc}/*-20260529.csv` → scanner 30s 后登记 3 个 file_record,各自带正确 tenant_id。

### 2.2 `GenerateStep.toStringKeyMap` 不支持 PG jsonb PGobject(L)

**问题**:`batch-worker-export/.../stage/GenerateStep.java` 的 `toStringKeyMap(Object)` 只识别 `Map` 和 `String`,jsonb 列经 MyBatis 返回 `org.postgresql.util.PGobject` 时 fallthrough 到 `Map.of()`,导致 `query_param_schema.export_data_ref` 永远读不到 → `export_data_ref is required` 误报。

**修复**:加 `PostgresqlJsonbTexts.tryExtract(raw)` 兜底反射抽 PGobject 的 JSON String(跟 `JdbcMappedImportSpec.toMap` 对齐)。

**验证**:EXPORT 立即跑通,MinIO outbound 落 csv。

### 2.3 fixture 约定:WorkflowNodeCode 严格匹配 START/END(M)

**问题**:`WorkflowNodeCode` 枚举只有 `START` / `END`(无 NODE_ 前缀)。DAG `resolveInitialNodes` 查 `workflow_edge.from_node_code = "START"`,fixture 写 `NODE_START` 时找不到 outgoing edge → initialNodes 空 → fallthrough 到 plan dispatch → workerGroup=WORKFLOW 选 worker 失败 → workflow 卡 CREATED。

**修复**:fixture 约定 node_code 必须严格 `START` / `END`(其他节点可自由命名)。

**验证**:改 SQL 节点 code 后,workflow_run 立即 SUCCESS,3 个 node_run 持久化,子 instance 自动 spawn。

**建议**:`PackageValidator` 加 fixture 校验,显式拒绝非 START/END 命名的开始/结束节点。

## 3. fixture 设计层面发现(非 bug,工程缺口)

| 项 | 现状 | 修法 |
|---|---|---|
| `pipeline_step_definition.step_params` | fixture 几乎全 `{}` | 真实业务参数应在 file_template_config(jdbcMappedImport / sql_template_export 都已支持) |
| TA_IMPORT_CUSTOMER 缺 PREPROCESS step | RECEIVE → PARSE 直跳 → file_state RECEIVED → PARSED 状态机禁止 | 加 PREPROCESS step,负责 RECEIVED → PARSING 转换 |
| TB_EXPORT_STATEMENT 只有 1 个 GENERATE step | 缺 PREPARE → templateConfig 没加载到 context → GenerateStep 找不到 export_data_ref | EXPORT pipeline 5-step 是必须 |
| job_definition.execution_mode | importer 不识别 column,导致 NOT NULL violation | importer 加 column 映射,或 SQL UPDATE 默认 'FULL' |
| EXPORT `default_query_sql` 必须含 `:batchNo` | `sql_template_export` validator 要求 named param | SQL 写 `WHERE tenant_id = :tenantId AND :batchNo IS NOT NULL` |
| ta/tb/tc fixture workflow_node 用 NODE_START | DAG 找不到 outgoing edge | 改 fixture xlsx 或加 PackageValidator 拦截 |
| ta channel_config SFTP 用 `host` key | worker 代码读 `sftp_host` key | 统一 key 命名(channel JSON 配置约定) |

## 4. fixture xlsx 改动(本次)

`docs/test-data/test-full-coverage-import-suite/{ta,tb,tc}-tenant-config-package-test.xlsx`:

- 清空 `workflow_definition` / `workflow_node` / `workflow_edge` 数据行(原数据用 NODE_START 命名,触发 DAG bug;后续若加回,用 START/END)
- channel_config:`SFTP target_endpoint` `sftp:22` → `192.168.1.15:12222`(host LAN);`mockserver:1080` → `192.168.1.15:11080`(多机环境必需)
- 改完未 commit(用户后续如要 commit,需先回滚 192.168.1.15 改成可配置占位)

## 5. 完整跑通的 setup 步骤(全本机)

```bash
# 0. 起 infra + sim 容器
docker compose up -d
bash scripts/sim/02-start-sim.sh

# 1. 起 7 个 BE(BATCH_WORKER_IMPORT_SCANNER_BIZ_DATE_PATTERN env 必须设)
export BATCH_WORKER_IMPORT_SCANNER_BIZ_DATE_PATTERN='(?<bizDate>\d{8})'
bash scripts/local/start-all.sh

# 2. 重启 orchestrator(修首次 NoClassDef,见 §6)
bash scripts/local/restart.sh orchestrator

# 3. 初始化 biz schema + MinIO bucket
bash scripts/sim/01-init-biz.sh

# 4. 登录 + 导租户(03 已被 user revert 回 bash 3.2 兼容性差版本,推荐 inline curl,见 commit 历史)
#    - 必须 SQL UPDATE batch.job_definition SET execution_mode='FULL' WHERE tenant_id IN ('ta','tb','tc')

# 5. 关键 SQL 配置(跑 IMPORT 前)
psql -f docs/test-data/sim-e2e-bootstrap.sql   # 见 §7 模板

# 6. 触发 → 验证(见 scripts/sim/06-verify.sh + §1 各场景脚本)
```

## 6. 已知坑

### 6.1 orchestrator 首启 NoClassDef

PG migration 未完成时启动 orch → Mapper Inner class load 失败 → KafkaListener 死。**症状**:`outbox_event does not exist`、`worker_registry does not exist`。**解决**:确认 docker postgres healthy 后,执行 `bash scripts/local/restart.sh orchestrator`。

### 6.2 fix 后的代码已写、未 commit

- `batch-worker-import/.../ImportIngressScanner.java`(多租户)
- `batch-worker-export/.../GenerateStep.java`(PG jsonb)

后续如 commit,建议:
- Scanner 多租户:加 `batch.worker.import.scanner.multi-tenant-mode` 开关,默认 false 保旧行为
- PG jsonb:无需开关(纯 bugfix)

### 6.3 worker-process plugin id 是 camelCase

`SqlTransformComputePlugin.PLUGIN_ID = "sqlTransformCompute"` — fixture / SQL 配 `impl_code` 时务必 camelCase,**不是** `sql_transform_compute`。

### 6.4 worker-dispatch SFTP/HTTP channel key 命名

| channel_type | config_json 必备 key | 注 |
|---|---|---|
| SFTP | `sftp_host` / `sftp_port` / `sftp_username` / `sftp_password` / `sftp_remote_path` | 不是 `host`/`port` |
| API_PUSH | `url` / `method` / `tokenHeader` | 这三个会被 `ChannelConfigMerge` 白名单忽略 → 但 dispatch 时仍读 `url` |

### 6.5 workflow_node 强约定

- `node_code = "START"` `node_type = "START"` (强匹配 enum)
- `node_code = "END"` `node_type = "END"`
- `workflow_edge.from_node_code = "START"` (不是 NODE_START)
- workflow job_definition `worker_group = NULL`(orchestrator 自管 DAG)
- `dag_enabled = true`

## 7. 端到端 SQL bootstrap 模板

下次跑前,03 导完 fixture 后执行(替代当前手术式 SQL):

```sql
-- 1) 修 execution_mode(fixture xlsx importer 未映射这列)
UPDATE batch.job_definition SET execution_mode = 'FULL' 
WHERE tenant_id IN ('ta','tb','tc') AND execution_mode IS NULL;

-- 2) 重建 IMPORT pipeline 5-step(fixture 4-step 缺 PREPROCESS)
-- 详见 verification 当天的 /tmp/fix-pipeline.sql

-- 3) 配 jdbcMappedImport spec(fixture file_template_config.query_param_schema 是空 {})
-- 详见 /tmp/fix-template.sql / fix-template2.sql

-- 4) 配 sql_template_export(EXPORT 模板)
-- 详见 /tmp/fix-export.sql / fix-tb-tc-export.sql

-- 5) 补 TB_EXPORT_STATEMENT pipeline 缺的 PREPARE/STORE/REGISTER/COMPLETE
-- 详见 /tmp/fix-tb-pipe.sql

-- 6) 补 ta SFTP channel sftp_host(替代 host)
UPDATE batch.file_channel_config SET config_json = jsonb_build_object(
    'sftp_host','192.168.1.15','sftp_port',12222,
    'sftp_username','ta','sftp_password','ta_pass_123',
    'sftp_remote_path','/inbound')
WHERE tenant_id='ta' AND channel_code='ta_sftp_inbound';

-- 7) workflow 节点 code 必须 START/END
-- 见 §6.5 / /tmp/wf-fix.sql
```

**建议**:把上面整理成 `docs/test-data/sim-e2e-bootstrap.sql` 一次 source 完事。

## 8. 验证产物快照

| 储存 | 内容 |
|---|---|
| `biz.customer_account` | ta 3 行(LARGE-* 50K + KILL-001 + CATCH-001 + RERUN-001) |
| `biz.transaction` | tb 3 行 |
| `biz.risk_score` | tc 3 行 |
| `biz.customer_processed` | ta 3 行(PROCESS 输出) |
| MinIO `outbound/REPORT/2026-05-29/.../ta_export_report.csv` | 376 B |
| MinIO `outbound/STATEMENT/2026-05-29/.../tb_export_statement.csv` | 284 B |
| MinIO `outbound/RISK_ALERT/2026-05-29/.../tc_export_risk_alert.csv` | 223 B |
| MockServer `/tc/ingest` | POST 1727 B 收件 |
| MockServer `/tb/callback` | POST 1750 B 收件 |
| `batch.workflow_run` | id=5 SUCCESS,id=6 FAILED |
| `batch.workflow_node_run` | 6 条(2 workflow × 3 node) |
| `batch.dead_letter_task` | 1681 条(异常自然累积) |
| `batch.file_record` | 70+ 条(scanner + worker + reconciler 三路登记) |

## 9. open backlog

| 优先级 | 项 | 说明 |
|---|---|---|
| P1 | Scanner 多租户 fix commit | 已写代码,加开关后 commit |
| P1 | GenerateStep PG jsonb fix commit | 已写代码,纯 bugfix |
| P2 | fixture importer 加 execution_mode column 支持 | 当前 NULL → constraint violation |
| P2 | EXPORT pipeline `default_query_sql` 必须 `:batchNo` validator 改 friendly error | 当前 `sql_template_export must reference named parameter :batchNo` 不直观 |
| P2 | PackageValidator 加 workflow START/END node 命名校验 | 避免下次撞 #2.3 |
| P3 | TB_EXPORT_STATEMENT 1-step pipeline 是 fixture 数据缺陷 | xlsx 补 5-step |
| P3 | dispatch channel `host` vs `sftp_host` key 统一 | 历史包袱,需文档 + ChannelConfigMerge 转换 |
| P3 | workflow GATEWAY / FAILURE edge 完整 e2e | L9 验证了 FAILED,但 FAILURE edge 跑分支 fallback 未覆盖 |

---

## 10. P0/P1/P2/P3 深度场景验证(2026-05-29 第 2 波)

### P0 业务核心

| # | 项 | 结果 |
|---|---|---|
| P0-1 | Workflow FAILURE edge | ✓ 真触发 NODE_PROCESS FAILED → DAG 走 FAILURE edge → NODE_FALLBACK 派发(spawn 第 2 个 child instance) |
| P0-2 | SCHEDULED trigger Quartz cron | ✓ batch.tenant 缺数据,SQL insert ta/tb/tc 后 reconciler register 2 trigger,SCHEDULED instance 自动 60s 精确触发(instance 98/99) |
| P0-3 | DISPATCH retry/compensate | ✓ 4-stage 链:PREPARE SUCCESS → DISPATCH FAILED → RETRY FAILED → COMPENSATE SUCCESS,dead_letter 累积 |
| P0-4 | IMPORT skip 阈值 | △ -D 参数注入 worker OK,但 LOAD 阶段 NOT NULL 优先 → skip 决策没机会跑;留 backlog |
| P0-5 | Multi-tenant 隔离 | ✓ SQL WHERE 带 tenant_id filter,id=tb's id AND tenant_id='ta' 返 0 行;admin 跨租户是设计(REVIEW),真 enforce 需 ROLE_TENANT_USER |

### P1 系统保护

| # | 项 | 结果 |
|---|---|---|
| P1-1 | RBAC 权限分级 | ✓ 3/3 场景:tenant-user 跨租户 GET(403 租户不匹配)+ tenant-user POST(403 访问被拒)+ auditor POST(403) |
| P1-2 | heartbeat silent timeout | ✓ SIGSTOP worker-import 60s(JVM 冻结 + heartbeat 停),instance 仍 SUCCESS,lease reclaim 自动 |
| P1-3 | shedlock 多实例互斥 | ✓ 起 2 worker-import 实例(node-1:18083 + node-2:18093),Redis lock 只 1 host(`ADDED@dz-Mac-mini.local`) |
| P1-4 | read replica 读写分离 | △ replica 容器 recovery=true,console-api dashboard SUCCESS(router 工作);streaming replication 没建立(留 backlog wal_keep_size + pg_basebackup) |
| P1-5 | Outbox republish | ✓ console-api `/ops/outbox/republish?tenantId=ta` body=[8185],outbox PUBLISHED publish_attempt=2 |
| P1-6 | Archive 冷热 | △ schema/code 完整(17 *_archive 表 + 3 cron scheduler),cron 周日 4:30 未到 |
| P1-7 | Tenant quota | ✓ strict policy max_running=1,并发 5 触发 → 5 SUCCESS + 1 WAITING(backpressure throttling) |

### P2 集成路径

| # | 项 | 结果 |
|---|---|---|
| P2-1 | console-api REST 接口 | ✓ 抽样 19 endpoints,9 HTTP 200(dashboard / file-templates / quota-policies / users / scheduler/snapshot / ops/triggers / admin/system/maintenance / meta/enums / batch-windows) |
| P2-2 | Audit log + traceId | ✓ console_operation_audit 8 条,action / operator_id / operator_role / trace_id / result 完整,@AuditAction 注解链路工作 |
| P2-3 | Webhook subscription | ✓ HTTPS profile check + SSRF DnsResolveGuard 私网拦 + eventTypes List schema 三层校验;本机无公网 callback 不能真 deliver |
| P2-4 | Forensic 取证 | ✓ ConsoleForensicController + ForensicExportRequest(tenantId/bizDateFrom/To/jobCodes/BUNDLE|JSON|CSV)接口完整 |
| P2-5 | dry-run via console | ✓ 之前 500 修了,`/api/console/jobs/trigger?dryRun=true` SUCCESS valid=true + 真触发也 SUCCESS |

### P3 显示

| # | 项 | 结果 |
|---|---|---|
| P3-1 | FE 起来 + LAN | ✓ vite preview --host 0.0.0.0,localhost+192.168.1.15:5173 都 200,首页 "批量调度平台",/api 反代生效 |
| P3-2 | i18n | ✓ messages.properties + messages_zh_CN.properties 1:1,默认 zh-CN 渲染 |
| P3-3 | Prometheus metrics | ✓ 4/4 BE 暴露 prometheus,orch 1815 条 metric,batch_ 业务前缀(SLA violation 731 / dead letter 65 / file arrival 系列) |

## 11. 第 2 波新增 BE 真问题 + 修复

### 11.1 P0-2 SCHEDULED 真因:`batch.tenant` 缺数据(SQL 补)

WheelTriggerReconciler SQL 用 `JOIN batch.tenant t ON t.tenant_id = jd.tenant_id AND t.status != 'SUSPENDED'`,fixture 没插 ta/tb/tc 到 tenant 表 → reconciler 跳过 → CRON job 永不注册。

SQL 一行修:
```sql
INSERT INTO batch.tenant (tenant_id, tenant_name, status) VALUES
  ('ta','tenant ta','ACTIVE'),
  ('tb','tenant tb','ACTIVE'),
  ('tc','tenant tc','ACTIVE') ON CONFLICT DO NOTHING;
```

**建议**:fixture xlsx 增加 `tenant` sheet,或 PackageValidator 加 fixture 一致性校验。

### 11.2 fixture cron 表达式格式错(observed,非 sim 引入)

fixture 默认 CRON 是 Linux 5-field(`0 2 * * *`),Quartz 要 6/7 field。reconciler 日志:
```
wheel reconcile skip trigger with invalid CRON expression: 
tenantId=ta jobCode=TA_IMPORT_CUSTOMER expr=[0 2 * * *]
(Quartz needs 6 or 7 fields; Linux 5-field form rejected)
```

**建议**:fixture xlsx 改 6-field(`0 0 2 * * ?`),或 PackageValidator 加 cron format check 拦截。

## 12. 第 2 波 dead letter 累计

| 来源 | 计数 |
|---|---|
| `batch.dead_letter_task` 总 | 1681 → 7454+(P0/P1 测试期间持续累积) |
| `JOB_PARTITION` reason | `DISPATCH_SEND_FAILED` / `BUSINESS_ERROR` / `EXPORT_GENERATE_FAILED` 全频段 |
| replay_status | `NEW`(自动 retry 路径就位) |

## 13. 最终累计

- **6 BE 模块** 全跑通(trigger/orch/4 worker)
- **25+ 真异常 + 系统保护场景**(L1-L9 + P0-1~5 + P1-1~7 + P2-1~5 + P3-1~3)
- **4 真 BE/fixture bug**:Scanner 多租户 + GenerateStep PG jsonb + WorkflowNodeCode 严格匹配 + batch.tenant 缺数据
- **2 真 BE 代码 fix**(Scanner + GenerateStep)进入 PR #77
- **200+ job_instance + 7000+ dead_letter + 1815 metric + 8 audit log**

## 14. open backlog(第 2 波)

| 优先级 | 项 | 说明 |
|---|---|---|
| P1 | fixture sim-e2e-bootstrap.sql 整理 | 把本次所有手术 SQL 一键化,避免下次重做 |
| P1 | fixture importer 加 execution_mode column 映射 | 否则 NOT NULL violation |
| P1 | PackageValidator 加 workflow START/END 命名校验 | 拦截 NODE_START / NODE_END 错命名 |
| P1 | fixture cron 表达式改 Quartz 6-field | reconciler 才能注册 |
| P2 | Workflow GATEWAY (AND/OR/N_OF join) + CONDITION edge | fixture 原有 TC_WF_GATEWAY 可复活 |
| P2 | DISPATCH 真 ACK / IMPORT preprocess 解密压缩 / IMPORT_FEEDBACK | step 真业务覆盖 |
| P2 | EXPORT jdbc_mapped_export batch+detail 双表 | 只测了 sql_template |
| P2 | PROCESS INCREMENTAL execution_mode + watermark | 只测了 FULL |
| P2 | EVENT / MANUAL trigger 类型 | 只测了 API/CATCH_UP/RERUN/SCHEDULED |
| P3 | stream replication 真建立 | wal_keep_size + pg_basebackup 流程 |
| P3 | archive 真触发(改 cron / 手工 archiveOnce) | cron 周日才跑,sim 不到 |
| P3 | webhook 真 deliver(公网 callback / SSRF 关) | 本机测不了真 HTTP send |
| P3 | Worker resource_tag selector / Maintenance bypass 切换 | 配置层验证 |

---

**验证执行**:2026-05-29(2 波)  
**总耗时**:~6 小时(sim 框架 → 业务跑通 → 异常 → P0/P1/P2/P3 系统验证)  
**修复 BE bug**:3 代码 + 1 fixture(Scanner 多租户 / GenerateStep PG jsonb / batch.tenant 缺 / workflow node code 约定)  
**累计触发**:200+ job_instance / 11 类 trigger 场景 / 5-stage WAP 工作流 / 7000+ dead letter / 1815 prometheus metric / 8 audit log / 9 workflow_node_run / 25+ 异常&系统保护场景
