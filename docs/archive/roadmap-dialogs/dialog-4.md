# dialog-4

本文档用于把 2026-03-22 这次“设计文档 / 对话记录 / 现有代码”三方核对后的**剩余缺口**，整理成后续可逐轮推进的对话脚本。

使用原则：

- 每一轮只收口一类能力，避免同时改太多面导致回归难查
- 每一轮都保留“用户 / 我 / 验收 / 状态”结构，便于继续接话
- 已经完成且不需要返工的大项，不再重复拆分到这里

## 当前基线

- 已较完整：调度主链、DAG 推进、分片、Outbox/Kafka、Worker 排空、文件导入/导出/分发主链、补偿统一入口、多租户与控制台查询基线
- 当前仍需继续补：KMS 全链路、可选前端 `Console Web`

## 第1轮：补 8.x quota_reset_policy 运行时闭环

- 用户：先补第1轮，把资源调度里 `quota_reset_policy` 从“字段已落库”补成真正可执行的运行时策略。
- 我：会补 `CALENDAR_DAY / SLIDING_WINDOW` 的计数口径、重置时机、落库字段和调度器联动，避免现在只有表字段和快照，没有真正的恢复逻辑。
- 验收：租户/队列额度会按策略自动恢复；控制台快照能看到当前计数和恢复口径；编译通过。
- 状态：已完成

## 第2轮：补 9.3 / 9.4 文件链高级能力

- 用户：先补第2轮，把文件链从“主链可用”补到更接近说明书完整版。
- 我：会先收口 `jdbc_mapped` 的任意列 Map 直通，减少 `CustomerImportPayload` 绑定限制；同时补导出格式矩阵，至少把 `JSON / DELIMITED` 之外的 Excel、定长、`quotePolicy / escapePolicy / headerRows` 配置面补齐。
- 验收：导入不再强依赖示例实体字段同名；导出格式和转义策略可配置；相关模板和示例可跑通。
- 状态：已完成

## 第3轮：补 9.12 边查边写与全链路 OOM 防线

- 用户：先补第3轮，把“部分流式”补成“主路径都不全量堆内存”。
- 我：会重点收口 `VALIDATE`、部分 JSON 导出、快照读取这几条仍可能受限的路径，明确哪些地方是分页、哪些地方是 chunk、哪些地方允许落临时文件，不再只做到 Parse 和部分 Generate。
- 验收：主链不再依赖全量 `List` 聚合；大文件场景的内存边界可解释；参数默认值和行为文档一致。
- 状态：已完成

## 第4轮：补 9.5 / 10.x 渠道健康治理与 NAS/OSS 真实适配

- 用户：先补第4轮，把分发侧从“熔断 + 部分真实渠道”补到可运营版。
- 我：会补 `NAS / OSS` 的真实适配器、远端健康探测、失败退避、恢复探测和更完整的回执/重试治理，不再让它们继续停留在 stub 文件信封模式。
- 验收：`NAS / OSS` 不再走 stub；渠道健康状态可查询；失败退避和恢复行为可审计。
- 状态：已完成

## 第5轮：补 12.x / 15.x 审批流产品化与文件安全闭环

- 用户：先补第5轮，把现在“带 `approvalId`”的能力补成真正的审批工作流。
- 我：会补统一审批单模型、状态机、查询与操作入口，把 catch-up、补偿、DLQ replay、下载审批这些高风险动作收口到同一条流程线上，而不是只在请求参数里传一个 `approvalId`。
- 验收：高风险动作先进入审批，再由受控接口执行；审批状态可查；补偿/下载/死信回放都能串到审批单。
- 状态：已完成

## 第6轮：补 15.2 KMS / 加解密全链路

- 用户：先补第6轮，把文件内容安全从“字段和门禁”补成真实的密钥闭环。
- 我：会补对象落盘加密、解密读取、密钥版本解析、轮换窗口和失败审计，明确 `content_encryption_enabled / encryption_key_ref` 的真实执行链路，而不只是在预签名时要求审批。
- 验收：文件对象加密和解密链路真实生效；密钥版本可追溯；轮换过程可审计、可回放。
- 状态：进行中
- 说明：已补平台级 `batch.security.testing-open` 开关，测试期可放开认证、脱敏与预解密门禁；真实 KMS 对象加解密链路继续补齐。

## 第7轮：补 16.x 可观测性自动化与统一日志管道

- 用户：先补第7轮，把当前“Prometheus / Grafana 基线 + runbook”补到更可交付的运维面。
- 我：会补统一 MDC 口径、日志采集示例、Alertmanager 路由模板、Kafka lag / 渠道健康 / 容量阈值告警规则，以及自动化巡检脚本，避免现在主要靠文档说明。
- 验收：核心服务日志字段一致；仓库内有可直接使用的告警模板和巡检脚本；关键运行指标有统一告警口径。
- 状态：已完成

## 第8轮：补 16.5 / 18.4 集成测试与失败演练

- 用户：先补第8轮，把“能编译”提升到“主链路可证明”。
- 我：会补 `SpringBootTest`、`Testcontainers`、`EmbeddedKafka` 或等价测试基建，覆盖触发、调度、执行、补偿、租约回收、文件治理和关键失败回放，不再只有一个 `FileStateMachineTest`。
- 验收：至少有主链路集成测试、失败回放测试和关键治理测试；CI 可执行；测试结果可复现。
- 状态：已完成（2026-03-28）

## 第9轮：补 20.x 压测脚本与容量基线

- 用户：先补第9轮，把评估章节里的“待压测确认”变成仓库内真实交付件。
- 我：会补压测脚本、压测说明、指标采集口径和容量基线文档，把 `job_instance_create_tps / partition_dispatch_tps / schedule_decision_p95_ms / mq_backlog_recovery_minutes` 这些关键指标至少形成一版基准数据。
- 验收：仓库内有压测脚本和说明；有一版容量基线文档；默认运行参数能和压测结果对应起来。
- 状态：已完成（2026-03-28）

## 第10轮：补 18.x 生产部署产物与灰度发布清单

- 用户：先补第10轮，把交付面从本地 `docker-compose` 补到生产级部署基线。
- 我：会补 `Dockerfile`、K8s/Helm 产物、环境变量清单、灰度发布顺序、回滚步骤和恢复手册，避免当前 runbook 里只描述过程但缺少可执行部署产物。
- 验收：仓库具备最小生产部署清单；滚动升级、灰度和回滚有明确产物支撑；不再只有本地依赖脚本。
- 状态：已完成（2026-03-28）

## 第11轮：补 3.x / 18.x 合规交付物

- 用户：先补第11轮，把合规要求补成正式仓库产物。
- 我：会补 `THIRD-PARTY-LICENSES.md`、`NOTICE`、`SBOM`，并把依赖许可证核查方式写清楚，避免说明书里有要求但工程里没有正式交付件。
- 验收：仓库内存在版本可对应的许可证清单、NOTICE 和 SBOM；新增依赖时有清晰的核查口径。
- 状态：已完成（2026-03-28）

## 第12轮：补 6.x / 17.x 可选前端 Console Web   TODO

- 用户：先补第12轮，把控制台从纯 API 查询/操作面扩到最小前端可视界面。
- 我：会把 `batch-console-web` 作为可选模块落地，优先承接 DAG 拓扑、文件链查询、Worker 运维、审批列表和关键治理操作，不改变现有后端 API 边界。
- 验收：仓库出现可运行的 `batch-console-web`；至少覆盖 DAG、文件、Worker、审批几个核心页面；现有 API 可直接复用。
- 状态：未开始

## 推荐推进顺序

1. 先补运行时和文件链硬缺口：第1轮到第4轮
2. 再补审批、安全和运维闭环：第5轮到第7轮
3. 最后补“能上线”的工程件：第8轮到第12轮
