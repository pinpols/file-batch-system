<?xml version="1.0"?>
<ruleset name="batch-platform"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

    <description>
        batch-platform PMD 规约 — 对齐 CLAUDE.md 编码规范
    </description>

    <!-- ============================================================
         CLAUDE.md: 禁止在代码中使用全限定类名（FQN）
         已 import 的类型不得再用完整包路径引用。
         ============================================================ -->
    <rule ref="category/java/codestyle.xml/UnnecessaryFullyQualifiedName"/>

    <!-- ============================================================
         CLAUDE.md: 方法参数数量不能超过 6 个
         minimum=7 => 参数 ≥ 7 时触发违规。
         构造器（record / DTO / Spring DI）按规约豁免，
         如需单独豁免请在构造器上添加：
             @SuppressWarnings("PMD.ExcessiveParameterList")
         ============================================================ -->
    <rule ref="category/java/design.xml/ExcessiveParameterList">
        <properties>
            <property name="minimum" value="7"/>
        </properties>
    </rule>

    <!-- ============================================================
         CLAUDE.md: 分支消除 — inline else 体 > 10 行须提取为命名方法
         ExcessiveMethodLength 作为代理信号：方法体过长通常意味着
         未消除的分支内联，阈值设置宽松，仅捕获明显超标情形。
         ============================================================ -->
    <rule ref="category/java/design.xml/NcssCount">
        <properties>
            <property name="methodReportLevel" value="60"/>
            <property name="classReportLevel" value="1500"/>
            <property name="ncssOptions" value=""/>
        </properties>
    </rule>

    <!-- ============================================================
         质量基线
         ============================================================ -->

    <!-- 空 catch 块：注释说明理由时豁免 -->
    <rule ref="category/java/errorprone.xml/EmptyCatchBlock">
        <properties>
            <property name="allowCommentedBlocks" value="true"/>
        </properties>
    </rule>

    <!-- 重复字符串字面量：≥ 5 次提取为常量，注解内豁免 -->
    <rule ref="category/java/errorprone.xml/AvoidDuplicateLiterals">
        <properties>
            <property name="maxDuplicateLiterals" value="4"/>
            <property name="skipAnnotations" value="true"/>
        </properties>
    </rule>

</ruleset>
