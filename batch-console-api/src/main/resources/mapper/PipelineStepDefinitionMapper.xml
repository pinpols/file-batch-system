<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.batch.console.mapper.PipelineStepDefinitionMapper">
    <select id="selectByPipelineDefinitionId" resultType="map">
        select
            id,
            pipeline_definition_id,
            step_code,
            step_name,
            stage_code,
            step_order,
            impl_code,
            step_params,
            timeout_seconds,
            retry_policy,
            retry_max_count,
            enabled,
            created_at,
            updated_at
        from batch.pipeline_step_definition
        where pipeline_definition_id = #{pipelineDefinitionId}
        order by step_order
    </select>

    <!--
        NOT NULL DEFAULT 列 fallback,避免 Excel parser 漏字段时 NULL 约束失败
        (timeout_seconds=0, retry_policy='NONE', retry_max_count=0, enabled=true)。
        与 batch.pipeline_step_definition 列 DEFAULT 同步。
    -->
    <insert id="insert" parameterType="map" useGeneratedKeys="true" keyProperty="id">
        insert into batch.pipeline_step_definition (
            pipeline_definition_id, step_code, step_name, stage_code,
            step_order, impl_code, step_params, timeout_seconds,
            retry_policy, retry_max_count, enabled
        ) values (
            #{pipeline_definition_id}, #{step_code}, #{step_name}, #{stage_code},
            #{step_order}, #{impl_code}, #{step_params}::jsonb,
            <choose><when test="timeout_seconds != null">#{timeout_seconds}</when><otherwise>0</otherwise></choose>,
            <choose><when test="retry_policy != null and retry_policy != ''">#{retry_policy}</when><otherwise>'NONE'</otherwise></choose>,
            <choose><when test="retry_max_count != null">#{retry_max_count}</when><otherwise>0</otherwise></choose>,
            <choose><when test="enabled != null">#{enabled}</when><otherwise>true</otherwise></choose>
        )
    </insert>

    <delete id="deleteByPipelineDefinitionId">
        delete from batch.pipeline_step_definition
        where pipeline_definition_id = #{pipelineDefinitionId}
    </delete>
</mapper>
