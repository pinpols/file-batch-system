<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.batch.console.mapper.BizTableSchemaQueryMapper">

    <select id="selectByTable" resultType="map">
        select schema_name as schemaName, table_name as tableName, columns::text as columns
        from batch.biz_table_schema
        where schema_name = #{schemaName} and table_name = #{tableName}
        limit 1
    </select>

    <select id="selectAll" resultType="map">
        select schema_name as schemaName, table_name as tableName, columns::text as columns
        from batch.biz_table_schema
        order by schema_name, table_name
    </select>

</mapper>
