<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.batch.console.mapper.OutboxEventMapper">

    <select id="countByStatus" resultType="long">
        select count(*)
        from batch.outbox_event
        where tenant_id = #{tenantId}
          and publish_status = #{publishStatus}
    </select>

    <select id="statsByStatus" resultType="map">
        select publish_status as "publishStatus",
               count(*)       as "count",
               min(created_at) as "oldest",
               max(created_at) as "newest"
        from batch.outbox_event
        where tenant_id = #{tenantId}
        group by publish_status
        order by publish_status
    </select>

    <!-- 故意只保留 SELECT — CLAUDE.md「Orchestrator 是唯一状态主机」硬约束下，console 不直接写
         outbox_event；cleanup / republish 由 orchestrator /internal/outbox/* 接口承接。 -->

</mapper>
