<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.batch.console.mapper.ResourceQueueMapper">
    <select id="selectByQuery" resultType="map">
        select
            id,
            tenant_id,
            queue_code,
            queue_name,
            queue_type,
            max_running_jobs,
            max_running_partitions,
            max_qps,
            worker_group,
            resource_tag,
            priority_policy,
            fair_share_weight,
            enabled,
            description,
            created_at,
            updated_at
        from batch.resource_queue
        where tenant_id = #{tenantId}
        <if test="queueCode != null and queueCode != ''">
            and queue_code = #{queueCode}
        </if>
        <if test="queueType != null and queueType != ''">
            and queue_type = #{queueType}
        </if>
        <if test="enabled != null">
            and enabled = #{enabled}
        </if>
        order by id desc
        <if test="pageRequest != null">
            <bind name="offset" value="(pageRequest.pageNo - 1) * pageRequest.pageSize"/>
            limit #{pageRequest.pageSize}
            offset #{offset}
        </if>
    </select>

    <select id="countByQuery" resultType="long">
        select count(1)
        from batch.resource_queue
        where tenant_id = #{tenantId}
        <if test="queueCode != null and queueCode != ''">
            and queue_code = #{queueCode}
        </if>
        <if test="queueType != null and queueType != ''">
            and queue_type = #{queueType}
        </if>
        <if test="enabled != null">
            and enabled = #{enabled}
        </if>
    </select>

    <select id="selectById" resultType="map">
        select *
        from batch.resource_queue
        where tenant_id = #{tenantId}
          and id = #{id}
    </select>

    <select id="selectByUniqueKey" resultType="map">
        select *
        from batch.resource_queue
        where tenant_id = #{tenantId}
          and queue_code = #{queueCode}
        limit 1
    </select>

    <insert id="insert" parameterType="map" useGeneratedKeys="true" keyProperty="id">
        insert into batch.resource_queue (
            tenant_id, queue_code, queue_name, queue_type,
            max_running_jobs, max_running_partitions, max_qps,
            worker_group, resource_tag, priority_policy, fair_share_weight,
            enabled, description
        ) values (
            #{tenant_id}, #{queue_code}, #{queue_name}, #{queue_type},
            #{max_running_jobs}, #{max_running_partitions}, #{max_qps},
            #{worker_group}, #{resource_tag}, #{priority_policy}, #{fair_share_weight},
            #{enabled}, #{description}
        )
    </insert>

    <update id="update" parameterType="map">
        update batch.resource_queue
        set queue_name = #{queue_name},
            queue_type = #{queue_type},
            max_running_jobs = #{max_running_jobs},
            max_running_partitions = #{max_running_partitions},
            max_qps = #{max_qps},
            worker_group = #{worker_group},
            resource_tag = #{resource_tag},
            priority_policy = #{priority_policy},
            fair_share_weight = #{fair_share_weight},
            enabled = #{enabled},
            description = #{description},
            updated_at = current_timestamp
        where tenant_id = #{tenant_id}
          and id = #{id}
    </update>

    <update id="toggleEnabled">
        update batch.resource_queue
        set enabled = #{enabled}, updated_at = current_timestamp
        where tenant_id = #{tenantId} and id = #{id}
    </update>
</mapper>
