<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.batch.console.mapper.TenantQuotaPolicyMapper">
    <select id="selectByQuery" resultType="map">
        select
            id,
            tenant_id,
            policy_code,
            max_running_jobs_per_tenant,
            max_partitions_per_tenant,
            max_qps_per_tenant,
            fair_share_weight,
            enabled,
            description,
            created_at,
            updated_at
        from batch.tenant_quota_policy
        where tenant_id = #{tenantId}
        <if test="policyCode != null and policyCode != ''">
            and policy_code = #{policyCode}
        </if>
        <if test="enabled != null">
            and enabled = #{enabled}
        </if>
        order by id desc
        <if test="pageRequest != null">
            <bind name="offset" value="(pageRequest.pageNo - 1) * pageRequest.pageSize"/>
            limit #{pageRequest.pageSize}
            offset #{offset}
        </if>
    </select>

    <select id="countByQuery" resultType="long">
        select count(1)
        from batch.tenant_quota_policy
        where tenant_id = #{tenantId}
        <if test="policyCode != null and policyCode != ''">
            and policy_code = #{policyCode}
        </if>
        <if test="enabled != null">
            and enabled = #{enabled}
        </if>
    </select>

    <select id="selectById" resultType="map">
        select *
        from batch.tenant_quota_policy
        where tenant_id = #{tenantId}
          and id = #{id}
    </select>

    <select id="selectByUniqueKey" resultType="map">
        select *
        from batch.tenant_quota_policy
        where tenant_id = #{tenantId}
          and policy_code = #{policyCode}
        limit 1
    </select>

    <insert id="upsertTenantQuotaPolicy" parameterType="com.example.batch.console.domain.param.TenantQuotaPolicyUpsertParam">
        insert into batch.tenant_quota_policy (
            tenant_id, policy_code, max_running_jobs_per_tenant,
            max_partitions_per_tenant, max_qps_per_tenant,
            fair_share_weight, enabled, description
        ) values (
            #{tenantId}, #{policyCode}, <choose><when test="maxRunningJobsPerTenant != null">#{maxRunningJobsPerTenant}</when><otherwise>0</otherwise></choose>,
            <choose><when test="maxPartitionsPerTenant != null">#{maxPartitionsPerTenant}</when><otherwise>0</otherwise></choose>, <choose><when test="maxQpsPerTenant != null">#{maxQpsPerTenant}</when><otherwise>0</otherwise></choose>,
            <choose><when test="fairShareWeight != null">#{fairShareWeight}</when><otherwise>1</otherwise></choose>, <choose><when test="enabled != null">#{enabled}</when><otherwise>true</otherwise></choose>, #{description}
        )
        on conflict (tenant_id, policy_code) do update
        set max_running_jobs_per_tenant = excluded.max_running_jobs_per_tenant,
            max_partitions_per_tenant = excluded.max_partitions_per_tenant,
            max_qps_per_tenant = excluded.max_qps_per_tenant,
            fair_share_weight = excluded.fair_share_weight,
            enabled = excluded.enabled,
            description = excluded.description,
            updated_at = current_timestamp
    </insert>

    <insert id="insert"
            parameterType="com.example.batch.console.domain.param.TenantQuotaPolicyUpsertParam">
        insert into batch.tenant_quota_policy (
            tenant_id, policy_code, max_running_jobs_per_tenant,
            max_partitions_per_tenant, max_qps_per_tenant,
            fair_share_weight, enabled, description
        ) values (
            #{tenantId}, #{policyCode}, <choose><when test="maxRunningJobsPerTenant != null">#{maxRunningJobsPerTenant}</when><otherwise>0</otherwise></choose>,
            <choose><when test="maxPartitionsPerTenant != null">#{maxPartitionsPerTenant}</when><otherwise>0</otherwise></choose>, <choose><when test="maxQpsPerTenant != null">#{maxQpsPerTenant}</when><otherwise>0</otherwise></choose>,
            <choose><when test="fairShareWeight != null">#{fairShareWeight}</when><otherwise>1</otherwise></choose>, <choose><when test="enabled != null">#{enabled}</when><otherwise>true</otherwise></choose>, #{description}
        )
    </insert>

    <update id="update"
            parameterType="com.example.batch.console.domain.param.TenantQuotaPolicyUpdateParam">
        update batch.tenant_quota_policy
        set max_running_jobs_per_tenant = #{maxRunningJobsPerTenant},
            max_partitions_per_tenant = #{maxPartitionsPerTenant},
            max_qps_per_tenant = #{maxQpsPerTenant},
            fair_share_weight = #{fairShareWeight},
            enabled = #{enabled},
            description = #{description},
            updated_at = current_timestamp
        where tenant_id = #{tenantId}
          and id = #{id}
    </update>

    <update id="toggleEnabled">
        update batch.tenant_quota_policy
        set enabled = #{enabled}, updated_at = current_timestamp
        where tenant_id = #{tenantId} and id = #{id}
    </update>
</mapper>
