spring:
  flyway:
    locations: classpath:db/migration

batch:
  worker:
    dispatch:
      heartbeat-interval-millis: 600000
      health:
        probe-interval-millis: 600000
      receipt-poll:
        interval-millis: 600000
