apiVersion: v2
name: batch-platform
description: File Batch Processing Platform — orchestrator, trigger, workers, console API
type: application
# chart 自身版本（修模板、values 结构、helper 时递增；不跟应用版本走）
version: 0.1.0
# 应用版本 —— 跟随 **最近一次 GA 版本**（与 values.yaml `image.tag` fallback 语义一致；
# 镜像 tag 默认从这里取，禁止落 SNAPSHOT 到 chart）。
# 发版流程：改 pom.xml <revision> 跑 release → 打稳定 tag → build 镜像 → 同步 appVersion。
# 详见 docs/runbook/releasing.md §Release flow。
appVersion: "1.1.0"
keywords:
  - batch
  - etl
  - file-processing
maintainers:
  - name: platform-team
