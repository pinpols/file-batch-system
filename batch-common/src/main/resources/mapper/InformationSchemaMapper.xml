<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.batch.common.mapper.InformationSchemaMapper">

    <select id="countSchema" resultType="int">
        SELECT count(*) FROM information_schema.schemata
         WHERE schema_name = #{schemaName}
    </select>

    <select id="countTable" resultType="int">
        SELECT count(*) FROM information_schema.tables
         WHERE table_schema = #{schemaName}
           AND table_name = #{tableName}
    </select>

    <select id="countColumn" resultType="int">
        SELECT count(*) FROM information_schema.columns
         WHERE table_schema = #{schemaName}
           AND table_name = #{tableName}
           AND column_name = #{columnName}
    </select>

    <select id="listColumns" resultType="java.lang.String">
        SELECT column_name FROM information_schema.columns
         WHERE table_schema = #{schemaName}
           AND table_name = #{tableName}
    </select>

    <!-- R7 DB 审计 P1-5：列出所有 ADD CONSTRAINT ... NOT VALID 后未补 VALIDATE 的约束。
         convalidated=false 的 CHECK / FK 表示 DB 允许新行违反约束直到运维手工 VALIDATE。 -->
    <select id="listInvalidConstraints" resultType="java.lang.String">
        SELECT n.nspname || '.' || rel.relname || '.' || c.conname
          FROM pg_constraint c
          JOIN pg_class rel ON rel.oid = c.conrelid
          JOIN pg_namespace n ON n.oid = rel.relnamespace
         WHERE c.convalidated = false
           AND n.nspname IN ('batch', 'archive')
         ORDER BY n.nspname, rel.relname, c.conname
    </select>

</mapper>
