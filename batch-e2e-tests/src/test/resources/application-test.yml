# 与 orchestrator 集成测试对齐：Flyway 使用 docs/sql/flyway；后台任务尽量静默。
spring:
  flyway:
    locations: classpath:db/migration

batch:
  sla:
    enabled: false
  scheduler:
    snapshot-persist-enabled: false
    snapshot-persist-ms: 600000
  worker:
    drain:
      enabled: false
  file-governance:
    latency:
      enabled: false
    archive:
      enabled: false
    reconcile:
      enabled: false
    arrival:
      enabled: false
    access:
      enabled: false
  outbox:
    poll-interval-millis: 600000
  retry:
    poll-interval-millis: 600000
  partition-lease:
    reclaim-interval-millis: 600000
  resource-scheduler:
    waiting-dispatch-interval-millis: 600000
    quota-reset-scan-interval-millis: 600000
    quota-reset-enabled: false
