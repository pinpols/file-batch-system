# biz shard-0 凭据模板 → 复制为 shard-0.env(已 gitignore)填真值。
# shard-0 = postgres-primary 的 batch_business(基线库);dev 默认账密如下,可直接用。
BIZ_SHARD_URL="jdbc:postgresql://localhost:15432/batch_business?currentSchema=biz&reWriteBatchedInserts=true"
BIZ_SHARD_USERNAME=batch_user
BIZ_SHARD_PASSWORD=batch_pass_123
