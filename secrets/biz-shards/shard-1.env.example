# biz shard-1 凭据模板 → 复制为 shard-1.env(已 gitignore)填真值。
# shard-1 = postgres-biz-shard-1(profile biz-shard);dev 默认账密如下,可直接用。
BIZ_SHARD_URL="jdbc:postgresql://localhost:15442/batch_business?currentSchema=biz&reWriteBatchedInserts=true"
BIZ_SHARD_USERNAME=batch_user
BIZ_SHARD_PASSWORD=batch_pass_123
