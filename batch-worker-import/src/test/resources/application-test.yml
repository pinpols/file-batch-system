spring:
  flyway:
    locations: classpath:db/migration

batch:
  worker:
    import:
      heartbeat-interval-millis: 600000
      scanner:
        enabled: false
        poll-interval-millis: 600000
