batch:
  worker:
    import:
      worker-type: IMPORT
      tenant-id: default-tenant
      file-processing:
        streaming-enabled: true
        page-size: 1000
        fetch-size: 1000
        chunk-size: 500
