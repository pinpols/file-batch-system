<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.batch.orchestrator.mapper.FileRecordLookupMapper">

    <!--
      Workflow 派发回退反查：partition.output_summary 不含 fileId 时使用。
      仅查 source_type='GENERATED' 行避免跨场景串号；ORDER BY id DESC LIMIT 1 取最新生成文件。
    -->
    <select id="selectIdByTenantAndTraceId" resultType="java.lang.Long">
        SELECT id
          FROM batch.file_record
         WHERE tenant_id = #{tenantId}
           AND trace_id = #{traceId}
           AND source_type = 'GENERATED'
         ORDER BY id DESC
         LIMIT 1
    </select>

    <select id="selectIdByTenantAndSourceRef" resultType="java.lang.Long">
        SELECT id
          FROM batch.file_record
         WHERE tenant_id = #{tenantId}
           AND source_ref = #{sourceRef}
           AND source_type = 'GENERATED'
         ORDER BY id DESC
         LIMIT 1
    </select>

</mapper>
